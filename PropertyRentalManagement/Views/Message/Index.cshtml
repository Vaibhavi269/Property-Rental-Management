@model IEnumerable<PropertyRentalManagement.Models.Message>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Msg_content)
        </th>
        <th>
            From
            @*@Html.DisplayNameFor(model => model.Person.FirstName)*@
        </th>
        <th>
            To
            @*@Html.DisplayNameFor(model => model.Person1.FirstName)*@
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Msg_content)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person1.FirstName)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.M_Id }) |
        @Html.ActionLink("Details", "Details", new { id=item.M_Id }) |*@
            @Html.ActionLink("Respond", "Create", new { id = item.M_Id }) | 
            @Html.ActionLink("Delete", "Delete", new { id = item.M_Id })
        </td>
    </tr>
}

</table>
